node_modules
.pnpm-store

# turbo
.turbo

# build outputs
dist
.next
out
build

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editors/os
.DS_Store
.vscode/*
!.vscode/extensions.json

# generated
# (packages/sdk/src/generated is committed on purpose — CI/Vercel builds import it
# without booting the API to regenerate it; run `pnpm generate:sdk` after API changes)
apps/api/openapi.json

tsconfig.build.tsbuildinfo